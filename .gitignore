# Secrets — never commit API keys
.env

# Local data (synced history, price cache, credentials, your CSV exports)
data/cache.json
data/prices.json
data/auth.json
data/imports/*.csv
data/manual.json

node_modules/
npm-debug.log*
